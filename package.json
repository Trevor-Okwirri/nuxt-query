{
  "name": "@trevorokwirri/nuxt-query",
  "type": "module",
  "version": "1.0.0",
  "description": "TanStack Query for Nuxt with SSR hydration and auto-imported composables",
  "keywords": [
    "nuxt",
    "nuxt-module",
    "tanstack-query",
    "vue-query",
    "data-fetching",
    "ssr"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs"
    },
    "./*": {
      "types": "./dist/runtime/*.d.ts",
      "import": "./dist/runtime/*.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "typesVersions": {
    "*": {
      ".": [
        "./dist/types.d.mts"
      ],
      "*": [
        "./dist/runtime/*",
        "./dist/*"
      ]
    }
  },
  "files": [
    "dist"
  ],
  "workspaces": [
    "playground"
  ],
  "scripts": {
    "check-types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit",
    "postinstall": "nuxt-module-build build --stub && nuxt-module-build prepare",
    "prepack": "nuxt-module-build build",
    "dev": "npm run dev:prepare && nuxt dev playground",
    "dev:build": "nuxt build playground",
    "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxt prepare playground",
    "release": "npm run lint && npm run test && npm run prepack && changelogen --release && npm publish && git push --follow-tags",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
  },
  "dependencies": {
    "@nuxt/kit": "^4.4.8",
    "@tanstack/match-sorter-utils": "^8.19.4",
    "@tanstack/query-core": "^5.102.8",
    "@vue/devtools-api": "^6.6.4",
    "vue-demi": "^0.14.10"
  },
  "devDependencies": {
    "@nuxt/a11y": "1.0.0-alpha.1",
    "@nuxt/devtools": "^3.2.4",
    "@nuxt/hints": "^1.1.4",
    "@nuxt/module-builder": "^1.0.2",
    "@nuxt/schema": "^4.4.8",
    "@nuxt/test-utils": "^4.0.2",
    "@types/node": "^25.6.0",
    "changelogen": "^0.6.2",
    "eslint": "^9.39.1",
    "nuxt": "4.4.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5",
    "vue": "^3.5.33",
    "vue-tsc": "^3.2.4"
  }
}
